# EduMatrix M1 阶段门禁审计

> 审计日期：2026-07-23  
> 权威标准：`outputs/竞品深度分析/05_EduMatrix完全体最终路线与全量实施计划.md` 第 6.5 节  
> 判定原则：只以当前代码、数据库约束、自动测试和可复现命令为证据；没有证据即不判定通过。

## 当前结论

M1 状态为 `COMPLETE`。S1-001~S1-044、G1~G9、M1 专项、全量后端、前端构建和真实模型最小连通性均已通过；`03_M1自动验收报告.json` 为 `automated_status=pass`、`manual_acceptance=not_required`。团队依照权威总方案的“等价证据不重复”规则取消原 M1 重复人工操作，`04_M1人工业务验收清单.md` 已改为关闭记录和证据映射；该判定不是人工通过声明。

## 门禁矩阵

| # | 门禁要求 | 状态 | 当前证据 | 尚需完成 |
|---|---|---|---|---|
| G1 | 全部核心对象具备数据库约束、仓储、API Schema 和权限测试 | PASS | 47 类对象统一网关，含 `profile_evidence`；数据库约束、Pydantic Schema、对象/课程/组织/内部权限测试；12 个 API/主应用文件直接 ORM 扫描无匹配；M1 专项 `43 passed`、全量后端 `200 passed, 1 skipped` | 后续新增 API 必须继续通过仓储或领域服务访问数据库 |
| G2 | 旧数据可迁移或由兼容层读取且不丢失 | PASS | `LegacyDataAdapter` 只读投影知识文档、笔记、计划、聊天和错题；画像证据保留旧 JSON 并提供幂等正式投影/兼容读取；备份恢复有 checksum | 全量回归继续验证，不删除旧表和旧字段；无用户映射的历史画像证据需后续治理 |
| G3 | 任意资源可回答创建者、所有者、来源、版本和访问者 | PASS | `/api/v1/objects/{type}/{id}` 对 47 类对象返回统一所有权、范围、来源、版本和访问策略，不返回敏感正文 | 保持新对象注册表同步 |
| G4 | 越权、并发、重复请求和迁移失败均有自动测试 | PASS | 跨用户/跨组织 403、乐观锁 409、任务幂等、重复导入、迁移账本幂等、空库迁移、画像证据幂等投影和 checksum 篡改拒绝均已测；全量后端 `200 passed, 1 skipped` | 保持迁移历史只追加 |
| G5 | trace_id 可还原画像、Agent、产物、质量、反馈和决策 | PASS | `trace_bundle` 聚合画像快照、画像证据、任务、Agent、产物版本、learning_event 反馈、质量、claim、辩论、决策和评测；安全汇总不返回证据正文；聚焦测试通过 | 后续真实闭环继续复用同一 trace_id |
| G6 | 主领域仅通过 domain_pack 接入，规则不散落 | PASS | 领域版本、岗位任务、技能标准、评测集由 `domain_pack/domain_version` 绑定；通用对象/权限服务不含 RAG 业务判定 | 后续阶段继续禁止在页面复制领域规则 |
| G7 | evaluation_case 输入/金标不可变，运行可区分代码/数据/模型变化 | PASS | 案例有版本、数据版本、内容哈希；运行绑定 code/data/model；数据库触发器禁止更新和删除，失败测试通过 | 新案例必须新建版本，不修改历史行 |
| G8 | 企业对象具有版本、权限和审计 | PASS | 统一企业写服务校验组织角色和引用边界；岗位/能力/方案版本化，批次与认证锁快照；组织、岗位、培训、证据、认证、免修和外部身份变更均审计 | 后续 API/集成必须调用统一写服务 |
| G9 | 批次锁版本；跨组织、外部 ID、高风险免修拒绝 | PASS | 批次快照锁定；跨组织读取与培训方案引用均拒绝；外部 subject 唯一且绑定需组织管理员；高风险禁止免修 | 后续集成仍不得信任外部请求中的组织/用户 ID |

## 验证命令

```powershell
$tests = @(Get-ChildItem -LiteralPath tests -Filter 'test_m1_*.py' | ForEach-Object { $_.FullName })
& .\.venv\Scripts\python.exe -m pytest -q $tests
npm.cmd --prefix frontend run build
& .\.venv\Scripts\python.exe scripts\verify_m1_acceptance.py --test-llm
```

## 声明边界

- 本矩阵不把“表已创建”当作门禁通过。
- 本矩阵不宣称真实企业 SSO、HR/LMS 已接入。
- 本矩阵不宣称幻觉率、适配率、覆盖率或其他比赛指标已达标。
- 人工展示项必须实际执行后才记录结果，不能用设计说明替代。
